{
  "name": "reader-service",
  "version": "1.0.0",
  "main": "dist/reader-service/src/index.js",
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf ./dist",
    "build": "tsc",
    "build:clean": "yarn clean && yarn build",
    "dev": "yarn build:clean && node ."
  },
  "dependencies": {
    "express": "^4.17.1",
    "reflect-metadata": "^0.1.13",
    "rmi": "^0.0.1",
    "trmi-redis": "^1.0.4"
  },
  "devDependencies": {
    "@types/express": "^4.17.12",
    "@types/node": "^15.6.1",
    "typescript": "^4.3.2"
  }
}
